# Run script
---
This script start the all dependencies installation, it had been maked for automatize the proces
of start up a new development environment when I change of distro.


Can read the dependencies file `deps.pkg` and start the installation of all the dependecies checking
the system repository manager and if use another manager as _snap_ or *flask*.

